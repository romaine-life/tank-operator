{
  "name": "tank-antigravity-runner",
  "private": true,
  "version": "0.1.0",
  "description": "Long-lived pod-side runner that drives the Antigravity CLI (agy, Gemini-Ultra) and publishes canonical Tank conversation events to the session bus.",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "node -e \"require('node:fs').rmSync('dist',{recursive:true,force:true})\" && tsc",
    "start": "node dist/index.js",
    "test": "node --test $(find dist -name '*.test.js' 2>/dev/null)"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@nats-io/jetstream": "^3.4.0",
    "@nats-io/transport-node": "^3.4.0",
    "prom-client": "^15.1.3"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
