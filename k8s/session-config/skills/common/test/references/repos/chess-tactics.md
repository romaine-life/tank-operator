# chess-tactics test-slot validation

Project: `chess-tactics`

Use Glimmung `deploy_image_to_test_slot` after the branch is pushed and CI has
built the proof image. Verify the slot's browser-visible app surface with
`inspect_browser_url`.
