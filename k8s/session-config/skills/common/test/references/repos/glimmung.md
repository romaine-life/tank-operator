# glimmung test-slot validation

Project: `glimmung`

Use Glimmung `deploy_image_to_test_slot` after the branch is pushed and CI has
built the proof image. Verify the deployed API or dashboard surface directly
from the slot URL and capture browser evidence when the change is visible.
