interface:
  display_name: "Test Drive"
  short_description: "Drive a live feature test with evidence"
  default_prompt: "Use $test-drive to validate this feature against the already-provisioned test slot with Playwright evidence (the slot is created for you — do not reserve one)."
