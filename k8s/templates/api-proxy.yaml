{{- define "tank-operator.apiProxyLeafSdsSecret" -}}
secrets:
  - name: api_proxy_leaf
    tls_certificate:
      certificate_chain:
        filename: /etc/envoy/tls/tls.crt
      private_key:
        filename: /etc/envoy/tls/tls.key
      watched_directory:
        path: /etc/envoy/tls
{{- end -}}
{{- define "tank-operator.apiProxyDownstreamTlsContext" -}}
common_tls_context:
  tls_certificate_sds_secret_configs:
    - name: api_proxy_leaf
{{- end -}}
{{- if not (eq (include "tank-operator.isTestEnv" .) "true") }}
{{- /*
Anthropic API proxy: Envoy in front of api.anthropic.com plus a small
ext_proc gRPC sidecar that injects the current real OAuth Authorization
on every request and single-flight-refreshes against platform.claude.com
on upstream 401.

Why this exists: the previous design had session pods cache an access
token until a locally-stamped expiry and a CronJob rotate refresh+access
on a clock. Anthropic invalidates the previous access token whenever the
refresh token rotates, so every cron tick guaranteed a 401 on any pod
holding a cached previous token — and claude does NOT auto-refresh on
401, it tells the user to /login. Putting a transparent inline proxy in
front of api.anthropic.com decouples token lifecycle from pod lifetime:
pods get a placeholder Bearer, the proxy strips it and stamps the real
one, and pods never participate in OAuth.

Why ext_proc rather than a FastAPI all-in-one: Envoy gives us mature
HTTP/2, streaming, retry-on-status, real upstream TLS validation, and
sane timeouts; the auth-injection logic is small enough to keep in a
sidecar without re-implementing the rest. A compact Python ext_proc
service is the smallest piece of code we can own that handles refresh
correctly — it doesn't process bodies, only headers.

CA reuse: session pods already trust `claude-oauth-ca` (mounted as
NODE_EXTRA_CA_CERTS by sessions.py for the platform.claude.com gateway).
We issue a second leaf from the same Issuer so pods trust this proxy's
api.anthropic.com cert without any new trust-store work in the image.
*/}}
{{- if eq (include "tank-operator.renderWarm" .) "true" }}
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: claude-api-proxy-leaf
  namespace: {{ include "tank-operator.orchestratorNamespace" . }}
spec:
  secretName: claude-api-proxy-leaf
  duration: 2160h  # 90 days
  renewBefore: 360h
  dnsNames:
    - api.anthropic.com
  issuerRef:
    name: claude-oauth-ca-issuer
    kind: Issuer
{{- end }}
---
{{- /*
SA carries no azure.workload.identity/client-id annotation. The WI
webhook only needs the pod-level label `azure.workload.identity/use=true`
to project the federated SA token; AZURE_CLIENT_ID + AZURE_TENANT_ID
arrive via envFrom from the ESO-synced config Secret below, so
DefaultAzureCredential can mint a federated KV token without the
annotation. Same pattern as the orchestrator Deployment.
*/}}
{{- if eq (include "tank-operator.renderWarm" .) "true" }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.apiProxy.serviceAccount }}
  namespace: {{ include "tank-operator.orchestratorNamespace" . }}
{{- end }}
---
{{- /*
Refresh-lease RBAC (#1079 item 6). Each ext_proc replica takes the
api-proxy-refresh-<kv-secret-name> Lease (scoped per credential CHAIN —
the two claude deployments share a provider name but rotate unrelated
chains) for the duration of one OAuth token rotation, so concurrent
replicas never double-spend a single-use refresh_token (provider reuse
detection can revoke the whole grant family). Losers wait for the winner's rotated blob to arrive via the
KV → ESO → file pipeline instead of calling the provider. The client
fails OPEN when this RBAC is absent — proxies keep rotating exactly as
before, but replicas > 1 then reintroduces the double-rotation risk, so
keep this binding in lockstep with apiProxy.replicas. Verbs are the
minimal get/create/update the lease client speaks; no resourceNames pin
because RBAC cannot name-scope create and the names derive per provider
(see api-proxy/src/tank_api_proxy/lease.py).
*/}}
{{- if eq (include "tank-operator.renderWarm" .) "true" }}
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: api-proxy-refresh-lease
  namespace: {{ include "tank-operator.orchestratorNamespace" . }}
rules:
  - apiGroups: ["coordination.k8s.io"]
    resources: ["leases"]
    verbs: ["get", "create", "update"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: api-proxy-refresh-lease
  namespace: {{ include "tank-operator.orchestratorNamespace" . }}
subjects:
  - kind: ServiceAccount
    name: {{ .Values.apiProxy.serviceAccount }}
    namespace: {{ include "tank-operator.orchestratorNamespace" . }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: api-proxy-refresh-lease
{{- end }}
---
{{- if eq (include "tank-operator.renderHot" .) "true" }}
apiVersion: v1
kind: Service
metadata:
  name: claude-api-proxy
  namespace: {{ include "tank-operator.orchestratorNamespace" . }}
  labels:
    app.kubernetes.io/name: claude-api-proxy
spec:
  selector:
    app.kubernetes.io/name: claude-api-proxy
  ports:
    - name: https
      port: 443
      targetPort: 8443
    # /metrics — scraped by the ServiceMonitor in observability.yaml.
    # Plain HTTP on the ext_proc sidecar's metrics port; the upstream
    # api.anthropic.com leg keeps using port 443 with the leaf TLS.
    - name: metrics
      port: 9100
      targetPort: metrics
{{- end }}
---
{{- /*
Envoy config. Top-of-config rationale:

  - HTTPS listener with the in-cluster CA leaf (api.anthropic.com SAN).
    Pods reach this via a hostAlias mapping api.anthropic.com to this
    Service's ClusterIP and trust the leaf via NODE_EXTRA_CA_CERTS.
  - Single upstream cluster `anthropic_api` to the real api.anthropic.com,
    HTTP/2, auto_sni, system trust store for upstream TLS verification.
  - One ext_proc filter on the request side that calls the local sidecar
    on 127.0.0.1:9000 — strips Authorization, injects the current Bearer.
    On response, the same filter peeks :status; the sidecar treats 401 as
    a refresh trigger.
  - Retry policy: retriable_status_codes=[401], num_retries=1. Envoy
    re-runs the filter chain on retry, so the second pass picks up the
    refreshed token automatically.
  - 600s per-request timeout: Anthropic's streaming responses can be
    long-lived (large outputs, slow tool turns).
  - access_log emits one line per HTTP transaction to stdout. Cheap,
    and the line includes the upstream Anthropic request_id when the
    response carries one — turning "which call 4xx'd?" into a
    `kubectl logs -c envoy` grep.
*/}}
{{- if eq (include "tank-operator.renderWarm" .) "true" }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: claude-api-proxy-envoy
  namespace: {{ include "tank-operator.orchestratorNamespace" . }}
data:
  envoy.yaml: |
    admin:
      address:
        socket_address: { address: 127.0.0.1, port_value: 9901 }
    static_resources:
{{ include "tank-operator.apiProxyLeafSdsSecret" . | nindent 6 }}
      listeners:
        - name: api_listener
          address:
            socket_address: { address: 0.0.0.0, port_value: 8443 }
          per_connection_buffer_limit_bytes: 1048576
          filter_chains:
            - transport_socket:
                name: envoy.transport_sockets.tls
                typed_config:
                  "@type": type.googleapis.com/envoy.extensions.transport_sockets.tls.v3.DownstreamTlsContext
{{ include "tank-operator.apiProxyDownstreamTlsContext" . | nindent 18 }}
              filters:
                - name: envoy.filters.network.http_connection_manager
                  typed_config:
                    "@type": type.googleapis.com/envoy.extensions.filters.network.http_connection_manager.v3.HttpConnectionManager
                    stat_prefix: api_proxy
                    codec_type: AUTO
                    stream_idle_timeout: 600s
                    request_timeout: 600s
                    access_log:
                      - name: envoy.access_loggers.stdout
                        typed_config:
                          "@type": type.googleapis.com/envoy.extensions.access_loggers.stream.v3.StdoutAccessLog
                          log_format:
                            text_format_source:
                              inline_string: "[%START_TIME%] %REQ(:METHOD)% %REQ(:PATH)% -> %RESPONSE_CODE% %RESPONSE_FLAGS% rx=%BYTES_RECEIVED% tx=%BYTES_SENT% dur_ms=%DURATION% req_id=%REQ(x-request-id)% anthropic_req_id=%RESP(request-id)% retry=%REQ(x-envoy-attempt-count)%\n"
                    common_http_protocol_options:
                      idle_timeout: 600s
                    http_filters:
                      - name: envoy.filters.http.ext_proc
                        typed_config:
                          "@type": type.googleapis.com/envoy.extensions.filters.http.ext_proc.v3.ExternalProcessor
                          grpc_service:
                            envoy_grpc:
                              cluster_name: ext_proc_local
                            timeout: 5s
                          failure_mode_allow: false
                          processing_mode:
                            request_header_mode: SEND
                            response_header_mode: SEND
                            request_body_mode: NONE
                            response_body_mode: NONE
                            request_trailer_mode: SKIP
                            response_trailer_mode: SKIP
                          message_timeout: 5s
                      - name: envoy.filters.http.router
                        typed_config:
                          "@type": type.googleapis.com/envoy.extensions.filters.http.router.v3.Router
                    route_config:
                      name: anthropic
                      virtual_hosts:
                        - name: anthropic
                          domains: ["*"]
                          routes:
                            - match: { prefix: "/" }
                              route:
                                cluster: anthropic_api
                                timeout: 600s
                                retry_policy:
                                  retry_on: "retriable-status-codes"
                                  retriable_status_codes: [401]
                                  num_retries: 1
                                  per_try_timeout: 600s
                                auto_host_rewrite: true
      clusters:
        - name: anthropic_api
          type: LOGICAL_DNS
          connect_timeout: 5s
          dns_lookup_family: V4_ONLY
          lb_policy: ROUND_ROBIN
          typed_extension_protocol_options:
            envoy.extensions.upstreams.http.v3.HttpProtocolOptions:
              "@type": type.googleapis.com/envoy.extensions.upstreams.http.v3.HttpProtocolOptions
              explicit_http_config:
                http2_protocol_options: {}
          load_assignment:
            cluster_name: anthropic_api
            endpoints:
              - lb_endpoints:
                  - endpoint:
                      address:
                        socket_address:
                          address: api.anthropic.com
                          port_value: 443
          transport_socket:
            name: envoy.transport_sockets.tls
            typed_config:
              "@type": type.googleapis.com/envoy.extensions.transport_sockets.tls.v3.UpstreamTlsContext
              sni: api.anthropic.com
              common_tls_context:
                validation_context:
                  trusted_ca:
                    filename: /etc/ssl/certs/ca-certificates.crt
        - name: ext_proc_local
          type: STATIC
          connect_timeout: 1s
          lb_policy: ROUND_ROBIN
          typed_extension_protocol_options:
            envoy.extensions.upstreams.http.v3.HttpProtocolOptions:
              "@type": type.googleapis.com/envoy.extensions.upstreams.http.v3.HttpProtocolOptions
              explicit_http_config:
                http2_protocol_options: {}
          load_assignment:
            cluster_name: ext_proc_local
            endpoints:
              - lb_endpoints:
                  - endpoint:
                      address:
                        socket_address:
                          address: 127.0.0.1
                          port_value: 9000
{{- end }}
---
{{- /*
Workload-identity config secret for the ext_proc sidecar. Same pattern
as the orchestrator's: ESO syncs AZURE_CLIENT_ID and AZURE_TENANT_ID
out of KV so DefaultAzureCredential can mint a federated KV token for
the rotated-blob write.
*/}}
{{- if eq (include "tank-operator.renderWarm" .) "true" }}
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: {{ include "tank-operator.apiProxyConfigSecret" . }}
  namespace: {{ include "tank-operator.orchestratorNamespace" . }}
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: {{ .Values.externalSecret.storeName }}
    kind: {{ .Values.externalSecret.storeKind }}
  target:
    name: {{ include "tank-operator.apiProxyConfigSecret" . }}
    creationPolicy: Owner
    deletionPolicy: Retain
  data:
    - secretKey: AZURE_CLIENT_ID
      remoteRef:
        key: {{ .Values.apiProxy.kvClientIdKey }}
        conversionStrategy: Default
        decodingStrategy: None
        metadataPolicy: None
        nullBytePolicy: Ignore
    - secretKey: AZURE_TENANT_ID
      remoteRef:
        key: {{ .Values.credentialRefresher.kvTenantIdKey }}
        conversionStrategy: Default
        decodingStrategy: None
        metadataPolicy: None
        nullBytePolicy: Ignore
    # Google installed-app OAuth client_secret for the antigravity proxy's
    # refresh exchange. KV-sourced so the literal never lands in git; only the
    # antigravity-api-proxy reads ANTIGRAVITY_CLIENT_SECRET (claude/codex
    # ext_proc ignore it).
    - secretKey: ANTIGRAVITY_CLIENT_SECRET
      remoteRef:
        key: {{ .Values.apiProxy.kvAntigravityClientSecretKey | default "antigravity-oauth-client-secret" }}
        conversionStrategy: Default
        decodingStrategy: None
        metadataPolicy: None
        nullBytePolicy: Ignore
{{- end }}
---
{{- if eq (include "tank-operator.renderHot" .) "true" }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: claude-api-proxy
  namespace: {{ include "tank-operator.orchestratorNamespace" . }}
  labels:
    app.kubernetes.io/name: claude-api-proxy
spec:
  replicas: {{ .Values.apiProxy.replicas | default 1 }}
  revisionHistoryLimit: 0
  selector:
    matchLabels:
      app.kubernetes.io/name: claude-api-proxy
  template:
    metadata:
      labels:
        app.kubernetes.io/name: claude-api-proxy
        azure.workload.identity/use: "true"
    spec:
      serviceAccountName: {{ .Values.apiProxy.serviceAccount }}
      containers:
        - name: envoy
          image: {{ .Values.apiProxy.envoyImage | quote }}
          args:
            - "-c"
            - "/etc/envoy/envoy.yaml"
            - "--service-cluster"
            - "claude-api-proxy"
          ports:
            - name: https
              containerPort: 8443
            - name: admin
              containerPort: 9901
          volumeMounts:
            - name: envoy-config
              mountPath: /etc/envoy
              readOnly: true
            - name: leaf-tls
              mountPath: /etc/envoy/tls
              readOnly: true
          readinessProbe:
            tcpSocket:
              port: 8443
            initialDelaySeconds: 3
            periodSeconds: 10
        - name: ext-proc
          image: "{{ .Values.apiProxy.image.repository }}:{{ .Values.apiProxy.image.tag }}"
          imagePullPolicy: {{ .Values.apiProxy.image.pullPolicy }}
          ports:
            - name: grpc
              containerPort: 9000
            - name: metrics
              containerPort: 9100
          env:
            - name: PROXY_PROVIDER
              value: claude
            - name: METRICS_PORT
              value: "9100"
            - name: CLAUDE_CREDENTIALS_FILE
              value: /etc/claude-credentials/{{ .Values.externalSecret.claudeCredentials.secretKey }}
            - name: AZURE_KEYVAULT_URL
              value: {{ .Values.credentialRefresher.keyVaultUrl | quote }}
            - name: CLAUDE_CREDENTIALS_KV_KEY
              value: {{ include "tank-operator.claudeCredentialsKvKey" . | quote }}
            - name: AZURE_FEDERATED_TOKEN_FILE
              value: /var/run/secrets/azure/tokens/azure-identity-token
            - name: AZURE_AUTHORITY_HOST
              value: https://login.microsoftonline.com/
          envFrom:
            - secretRef:
                name: {{ include "tank-operator.apiProxyConfigSecret" . }}
          volumeMounts:
            - name: claude-credentials
              mountPath: /etc/claude-credentials
              readOnly: true
      volumes:
        - name: envoy-config
          configMap:
            name: claude-api-proxy-envoy
        - name: leaf-tls
          secret:
            secretName: claude-api-proxy-leaf
            optional: true  # cert-manager hasn't issued yet on first install
        - name: claude-credentials
          secret:
            secretName: {{ include "tank-operator.claudeCredentialsSecret" . }}
            optional: true
{{- end }}
---
{{- if eq (include "tank-operator.renderWarm" .) "true" }}
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: claude-secondary-api-proxy-leaf
  namespace: {{ include "tank-operator.orchestratorNamespace" . }}
spec:
  secretName: claude-secondary-api-proxy-leaf
  duration: 2160h  # 90 days
  renewBefore: 360h
  dnsNames:
    - api.anthropic.com
  issuerRef:
    name: claude-oauth-ca-issuer
    kind: Issuer
{{- end }}
---
{{- if eq (include "tank-operator.renderHot" .) "true" }}
apiVersion: v1
kind: Service
metadata:
  name: claude-secondary-api-proxy
  namespace: {{ include "tank-operator.orchestratorNamespace" . }}
  labels:
    app.kubernetes.io/name: claude-secondary-api-proxy
spec:
  selector:
    app.kubernetes.io/name: claude-secondary-api-proxy
  ports:
    - name: https
      port: 443
      targetPort: 8443
    - name: metrics
      port: 9100
      targetPort: metrics
{{- end }}
---
{{- if eq (include "tank-operator.renderWarm" .) "true" }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: claude-secondary-api-proxy-envoy
  namespace: {{ include "tank-operator.orchestratorNamespace" . }}
data:
  envoy.yaml: |
    admin:
      address:
        socket_address: { address: 127.0.0.1, port_value: 9901 }
    static_resources:
{{ include "tank-operator.apiProxyLeafSdsSecret" . | nindent 6 }}
      listeners:
        - name: api_listener
          address:
            socket_address: { address: 0.0.0.0, port_value: 8443 }
          per_connection_buffer_limit_bytes: 1048576
          filter_chains:
            - transport_socket:
                name: envoy.transport_sockets.tls
                typed_config:
                  "@type": type.googleapis.com/envoy.extensions.transport_sockets.tls.v3.DownstreamTlsContext
{{ include "tank-operator.apiProxyDownstreamTlsContext" . | nindent 18 }}
              filters:
                - name: envoy.filters.network.http_connection_manager
                  typed_config:
                    "@type": type.googleapis.com/envoy.extensions.filters.network.http_connection_manager.v3.HttpConnectionManager
                    stat_prefix: api_proxy
                    codec_type: AUTO
                    stream_idle_timeout: 600s
                    request_timeout: 600s
                    access_log:
                      - name: envoy.access_loggers.stdout
                        typed_config:
                          "@type": type.googleapis.com/envoy.extensions.access_loggers.stream.v3.StdoutAccessLog
                          log_format:
                            text_format_source:
                              inline_string: "[%START_TIME%] %REQ(:METHOD)% %REQ(:PATH)% -> %RESPONSE_CODE% %RESPONSE_FLAGS% rx=%BYTES_RECEIVED% tx=%BYTES_SENT% dur_ms=%DURATION% req_id=%REQ(x-request-id)% anthropic_req_id=%RESP(request-id)% retry=%REQ(x-envoy-attempt-count)%\n"
                    common_http_protocol_options:
                      idle_timeout: 600s
                    http_filters:
                      - name: envoy.filters.http.ext_proc
                        typed_config:
                          "@type": type.googleapis.com/envoy.extensions.filters.http.ext_proc.v3.ExternalProcessor
                          grpc_service:
                            envoy_grpc:
                              cluster_name: ext_proc_local
                            timeout: 5s
                          failure_mode_allow: false
                          processing_mode:
                            request_header_mode: SEND
                            response_header_mode: SEND
                            request_body_mode: NONE
                            response_body_mode: NONE
                            request_trailer_mode: SKIP
                            response_trailer_mode: SKIP
                          message_timeout: 5s
                      - name: envoy.filters.http.router
                        typed_config:
                          "@type": type.googleapis.com/envoy.extensions.filters.http.router.v3.Router
                    route_config:
                      name: anthropic
                      virtual_hosts:
                        - name: anthropic
                          domains: ["*"]
                          routes:
                            - match: { prefix: "/" }
                              route:
                                cluster: anthropic_api
                                timeout: 600s
                                retry_policy:
                                  retry_on: "retriable-status-codes"
                                  retriable_status_codes: [401]
                                  num_retries: 1
                                  per_try_timeout: 600s
                                auto_host_rewrite: true
      clusters:
        - name: anthropic_api
          type: LOGICAL_DNS
          connect_timeout: 5s
          dns_lookup_family: V4_ONLY
          lb_policy: ROUND_ROBIN
          typed_extension_protocol_options:
            envoy.extensions.upstreams.http.v3.HttpProtocolOptions:
              "@type": type.googleapis.com/envoy.extensions.upstreams.http.v3.HttpProtocolOptions
              explicit_http_config:
                http2_protocol_options: {}
          load_assignment:
            cluster_name: anthropic_api
            endpoints:
              - lb_endpoints:
                  - endpoint:
                      address:
                        socket_address:
                          address: api.anthropic.com
                          port_value: 443
          transport_socket:
            name: envoy.transport_sockets.tls
            typed_config:
              "@type": type.googleapis.com/envoy.extensions.transport_sockets.tls.v3.UpstreamTlsContext
              sni: api.anthropic.com
              common_tls_context:
                validation_context:
                  trusted_ca:
                    filename: /etc/ssl/certs/ca-certificates.crt
        - name: ext_proc_local
          type: STATIC
          connect_timeout: 1s
          lb_policy: ROUND_ROBIN
          typed_extension_protocol_options:
            envoy.extensions.upstreams.http.v3.HttpProtocolOptions:
              "@type": type.googleapis.com/envoy.extensions.upstreams.http.v3.HttpProtocolOptions
              explicit_http_config:
                http2_protocol_options: {}
          load_assignment:
            cluster_name: ext_proc_local
            endpoints:
              - lb_endpoints:
                  - endpoint:
                      address:
                        socket_address:
                          address: 127.0.0.1
                          port_value: 9000
{{- end }}
---
{{- if eq (include "tank-operator.renderHot" .) "true" }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: claude-secondary-api-proxy
  namespace: {{ include "tank-operator.orchestratorNamespace" . }}
  labels:
    app.kubernetes.io/name: claude-secondary-api-proxy
spec:
  replicas: {{ .Values.apiProxy.replicas | default 1 }}
  revisionHistoryLimit: 0
  selector:
    matchLabels:
      app.kubernetes.io/name: claude-secondary-api-proxy
  template:
    metadata:
      labels:
        app.kubernetes.io/name: claude-secondary-api-proxy
        azure.workload.identity/use: "true"
    spec:
      serviceAccountName: {{ .Values.apiProxy.serviceAccount }}
      containers:
        - name: envoy
          image: {{ .Values.apiProxy.envoyImage | quote }}
          args:
            - "-c"
            - "/etc/envoy/envoy.yaml"
            - "--service-cluster"
            - "claude-secondary-api-proxy"
          ports:
            - name: https
              containerPort: 8443
            - name: admin
              containerPort: 9901
          volumeMounts:
            - name: envoy-config
              mountPath: /etc/envoy
              readOnly: true
            - name: leaf-tls
              mountPath: /etc/envoy/tls
              readOnly: true
          readinessProbe:
            tcpSocket:
              port: 8443
            initialDelaySeconds: 3
            periodSeconds: 10
        - name: ext-proc
          image: "{{ .Values.apiProxy.image.repository }}:{{ .Values.apiProxy.image.tag }}"
          imagePullPolicy: {{ .Values.apiProxy.image.pullPolicy }}
          ports:
            - name: grpc
              containerPort: 9000
            - name: metrics
              containerPort: 9100
          env:
            - name: PROXY_PROVIDER
              value: claude
            - name: METRICS_PORT
              value: "9100"
            - name: CLAUDE_CREDENTIALS_FILE
              value: /etc/claude-credentials/{{ .Values.externalSecret.claudeSecondaryCredentials.secretKey }}
            - name: AZURE_KEYVAULT_URL
              value: {{ .Values.credentialRefresher.keyVaultUrl | quote }}
            - name: CLAUDE_CREDENTIALS_KV_KEY
              value: {{ include "tank-operator.claudeSecondaryCredentialsKvKey" . | quote }}
            - name: AZURE_FEDERATED_TOKEN_FILE
              value: /var/run/secrets/azure/tokens/azure-identity-token
            - name: AZURE_AUTHORITY_HOST
              value: https://login.microsoftonline.com/
          envFrom:
            - secretRef:
                name: {{ include "tank-operator.apiProxyConfigSecret" . }}
          volumeMounts:
            - name: claude-credentials
              mountPath: /etc/claude-credentials
              readOnly: true
      volumes:
        - name: envoy-config
          configMap:
            name: claude-secondary-api-proxy-envoy
        - name: leaf-tls
          secret:
            secretName: claude-secondary-api-proxy-leaf
            optional: true
        - name: claude-credentials
          secret:
            secretName: {{ include "tank-operator.claudeSecondaryCredentialsSecret" . }}
            optional: true
{{- end }}
---
{{- /*
Codex API proxy: same Envoy/ext_proc image as the Anthropic proxy, but
fronts chatgpt.com/backend-api/codex and owns the ChatGPT OAuth refresh
chain. Codex session pods use a placeholder chatgptAuthTokens auth.json;
the proxy injects the real Authorization and ChatGPT-Account-ID headers
from the centrally mounted codex-credentials Secret.
*/}}
{{- if eq (include "tank-operator.renderWarm" .) "true" }}
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: codex-api-proxy-leaf
  namespace: {{ include "tank-operator.orchestratorNamespace" . }}
spec:
  secretName: codex-api-proxy-leaf
  duration: 2160h
  renewBefore: 360h
  dnsNames:
    - chatgpt.com
  issuerRef:
    name: claude-oauth-ca-issuer
    kind: Issuer
{{- end }}
---
{{- if eq (include "tank-operator.renderHot" .) "true" }}
apiVersion: v1
kind: Service
metadata:
  name: codex-api-proxy
  namespace: {{ include "tank-operator.orchestratorNamespace" . }}
  labels:
    app.kubernetes.io/name: codex-api-proxy
spec:
  selector:
    app.kubernetes.io/name: codex-api-proxy
  ports:
    - name: https
      port: 443
      targetPort: 8443
    - name: metrics
      port: 9100
      targetPort: metrics
{{- end }}
---
{{- if eq (include "tank-operator.renderWarm" .) "true" }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: codex-api-proxy-envoy
  namespace: {{ include "tank-operator.orchestratorNamespace" . }}
data:
  envoy.yaml: |
    admin:
      address:
        socket_address: { address: 127.0.0.1, port_value: 9901 }
    static_resources:
{{ include "tank-operator.apiProxyLeafSdsSecret" . | nindent 6 }}
      listeners:
        - name: api_listener
          address:
            socket_address: { address: 0.0.0.0, port_value: 8443 }
          per_connection_buffer_limit_bytes: 1048576
          filter_chains:
            - transport_socket:
                name: envoy.transport_sockets.tls
                typed_config:
                  "@type": type.googleapis.com/envoy.extensions.transport_sockets.tls.v3.DownstreamTlsContext
{{ include "tank-operator.apiProxyDownstreamTlsContext" . | nindent 18 }}
              filters:
                - name: envoy.filters.network.http_connection_manager
                  typed_config:
                    "@type": type.googleapis.com/envoy.extensions.filters.network.http_connection_manager.v3.HttpConnectionManager
                    stat_prefix: codex_api_proxy
                    codec_type: AUTO
                    stream_idle_timeout: 600s
                    request_timeout: 600s
                    upgrade_configs:
                      - upgrade_type: websocket
                    access_log:
                      - name: envoy.access_loggers.stdout
                        typed_config:
                          "@type": type.googleapis.com/envoy.extensions.access_loggers.stream.v3.StdoutAccessLog
                          log_format:
                            text_format_source:
                              inline_string: "[%START_TIME%] %REQ(:METHOD)% %REQ(:PATH)% -> %RESPONSE_CODE% %RESPONSE_FLAGS% rx=%BYTES_RECEIVED% tx=%BYTES_SENT% dur_ms=%DURATION% req_id=%REQ(x-request-id)% retry=%REQ(x-envoy-attempt-count)%\n"
                    common_http_protocol_options:
                      idle_timeout: 600s
                    http_filters:
                      - name: envoy.filters.http.ext_proc
                        typed_config:
                          "@type": type.googleapis.com/envoy.extensions.filters.http.ext_proc.v3.ExternalProcessor
                          grpc_service:
                            envoy_grpc:
                              cluster_name: ext_proc_local
                            timeout: 5s
                          failure_mode_allow: false
                          processing_mode:
                            request_header_mode: SEND
                            response_header_mode: SEND
                            request_body_mode: NONE
                            response_body_mode: NONE
                            request_trailer_mode: SKIP
                            response_trailer_mode: SKIP
                          message_timeout: 5s
                      - name: envoy.filters.http.router
                        typed_config:
                          "@type": type.googleapis.com/envoy.extensions.filters.http.router.v3.Router
                    route_config:
                      name: chatgpt
                      virtual_hosts:
                        - name: chatgpt
                          domains: ["*"]
                          routes:
                            - match: { prefix: "/" }
                              route:
                                cluster: chatgpt_api
                                timeout: 600s
                                retry_policy:
                                  retry_on: "retriable-status-codes"
                                  retriable_status_codes: [401]
                                  num_retries: 1
                                  per_try_timeout: 600s
                                auto_host_rewrite: true
      clusters:
        - name: chatgpt_api
          type: LOGICAL_DNS
          connect_timeout: 5s
          dns_lookup_family: V4_ONLY
          lb_policy: ROUND_ROBIN
          load_assignment:
            cluster_name: chatgpt_api
            endpoints:
              - lb_endpoints:
                  - endpoint:
                      address:
                        socket_address:
                          address: chatgpt.com
                          port_value: 443
          transport_socket:
            name: envoy.transport_sockets.tls
            typed_config:
              "@type": type.googleapis.com/envoy.extensions.transport_sockets.tls.v3.UpstreamTlsContext
              sni: chatgpt.com
              common_tls_context:
                validation_context:
                  trusted_ca:
                    filename: /etc/ssl/certs/ca-certificates.crt
        - name: ext_proc_local
          type: STATIC
          connect_timeout: 1s
          lb_policy: ROUND_ROBIN
          typed_extension_protocol_options:
            envoy.extensions.upstreams.http.v3.HttpProtocolOptions:
              "@type": type.googleapis.com/envoy.extensions.upstreams.http.v3.HttpProtocolOptions
              explicit_http_config:
                http2_protocol_options: {}
          load_assignment:
            cluster_name: ext_proc_local
            endpoints:
              - lb_endpoints:
                  - endpoint:
                      address:
                        socket_address:
                          address: 127.0.0.1
                          port_value: 9000
{{- end }}
---
{{- if eq (include "tank-operator.renderHot" .) "true" }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: codex-api-proxy
  namespace: {{ include "tank-operator.orchestratorNamespace" . }}
  labels:
    app.kubernetes.io/name: codex-api-proxy
spec:
  replicas: {{ .Values.apiProxy.replicas | default 1 }}
  revisionHistoryLimit: 0
  selector:
    matchLabels:
      app.kubernetes.io/name: codex-api-proxy
  template:
    metadata:
      labels:
        app.kubernetes.io/name: codex-api-proxy
        azure.workload.identity/use: "true"
    spec:
      serviceAccountName: {{ .Values.apiProxy.serviceAccount }}
      containers:
        - name: envoy
          image: {{ .Values.apiProxy.envoyImage | quote }}
          args:
            - "-c"
            - "/etc/envoy/envoy.yaml"
            - "--service-cluster"
            - "codex-api-proxy"
          ports:
            - name: https
              containerPort: 8443
            - name: admin
              containerPort: 9901
          volumeMounts:
            - name: envoy-config
              mountPath: /etc/envoy
              readOnly: true
            - name: leaf-tls
              mountPath: /etc/envoy/tls
              readOnly: true
          readinessProbe:
            tcpSocket:
              port: 8443
            initialDelaySeconds: 3
            periodSeconds: 10
        - name: ext-proc
          image: "{{ .Values.apiProxy.image.repository }}:{{ .Values.apiProxy.image.tag }}"
          imagePullPolicy: {{ .Values.apiProxy.image.pullPolicy }}
          ports:
            - name: grpc
              containerPort: 9000
            - name: metrics
              containerPort: 9100
          env:
            - name: PROXY_PROVIDER
              value: codex
            - name: METRICS_PORT
              value: "9100"
            - name: CODEX_CREDENTIALS_FILE
              value: /etc/codex-credentials/{{ .Values.externalSecret.codexCredentials.secretKey }}
            - name: AZURE_KEYVAULT_URL
              value: {{ .Values.credentialRefresher.keyVaultUrl | quote }}
            - name: CODEX_CREDENTIALS_KV_KEY
              value: {{ include "tank-operator.codexCredentialsKvKey" . | quote }}
            - name: AZURE_FEDERATED_TOKEN_FILE
              value: /var/run/secrets/azure/tokens/azure-identity-token
            - name: AZURE_AUTHORITY_HOST
              value: https://login.microsoftonline.com/
          envFrom:
            - secretRef:
                name: {{ include "tank-operator.apiProxyConfigSecret" . }}
          volumeMounts:
            - name: codex-credentials
              mountPath: /etc/codex-credentials
              readOnly: true
      volumes:
        - name: envoy-config
          configMap:
            name: codex-api-proxy-envoy
        - name: leaf-tls
          secret:
            secretName: codex-api-proxy-leaf
            optional: true
        - name: codex-credentials
          secret:
            secretName: {{ include "tank-operator.codexCredentialsSecret" . }}
            optional: true
{{- end }}
---
{{- /*
Antigravity API proxy: same Envoy/ext_proc image as the Claude/Codex
proxies, fronting cloudcode-pa.googleapis.com (the Google Code Assist data
plane that agy / Gemini-Ultra calls). It owns the Google OAuth refresh
chain so the agy session pod never holds the refresh token — agy gets a
placeholder Bearer + far-future expiry, and this proxy injects the real
access token, single-flight-refreshing against oauth2.googleapis.com on
upstream 401. agy is a Go binary, so session pods trust this leaf via the
system trust store (SSL_CERT_FILE), not NODE_EXTRA_CA_CERTS. Same CA
(claude-oauth-ca) as the other two proxies — one more leaf, no new trust
work in the image.
*/}}
{{- if eq (include "tank-operator.renderWarm" .) "true" }}
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: antigravity-api-proxy-leaf
  namespace: {{ include "tank-operator.orchestratorNamespace" . }}
spec:
  secretName: antigravity-api-proxy-leaf
  duration: 2160h
  renewBefore: 360h
  dnsNames:
    - cloudcode-pa.googleapis.com
    - daily-cloudcode-pa.googleapis.com
    - play.googleapis.com
    - www.googleapis.com
    - oauth2.googleapis.com
  issuerRef:
    name: claude-oauth-ca-issuer
    kind: Issuer
{{- end }}
---
{{- if eq (include "tank-operator.renderHot" .) "true" }}
apiVersion: v1
kind: Service
metadata:
  name: antigravity-api-proxy
  namespace: {{ include "tank-operator.orchestratorNamespace" . }}
  labels:
    app.kubernetes.io/name: antigravity-api-proxy
spec:
  selector:
    app.kubernetes.io/name: antigravity-api-proxy
  ports:
    - name: https
      port: 443
      targetPort: 8443
    - name: metrics
      port: 9100
      targetPort: metrics
{{- end }}
---
{{- if eq (include "tank-operator.renderWarm" .) "true" }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: antigravity-api-proxy-envoy
  namespace: {{ include "tank-operator.orchestratorNamespace" . }}
data:
  envoy.yaml: |
    admin:
      address:
        socket_address: { address: 127.0.0.1, port_value: 9901 }
    static_resources:
{{ include "tank-operator.apiProxyLeafSdsSecret" . | nindent 6 }}
      listeners:
        - name: api_listener
          address:
            socket_address: { address: 0.0.0.0, port_value: 8443 }
          per_connection_buffer_limit_bytes: 1048576
          filter_chains:
            - transport_socket:
                name: envoy.transport_sockets.tls
                typed_config:
                  "@type": type.googleapis.com/envoy.extensions.transport_sockets.tls.v3.DownstreamTlsContext
{{ include "tank-operator.apiProxyDownstreamTlsContext" . | nindent 18 }}
              filters:
                - name: envoy.filters.network.http_connection_manager
                  typed_config:
                    "@type": type.googleapis.com/envoy.extensions.filters.network.http_connection_manager.v3.HttpConnectionManager
                    stat_prefix: antigravity_api_proxy
                    codec_type: AUTO
                    stream_idle_timeout: 600s
                    request_timeout: 600s
                    access_log:
                      - name: envoy.access_loggers.stdout
                        typed_config:
                          "@type": type.googleapis.com/envoy.extensions.access_loggers.stream.v3.StdoutAccessLog
                          log_format:
                            text_format_source:
                              inline_string: "[%START_TIME%] %REQ(:METHOD)% %REQ(:PATH)% -> %RESPONSE_CODE% %RESPONSE_FLAGS% rx=%BYTES_RECEIVED% tx=%BYTES_SENT% dur_ms=%DURATION% req_id=%REQ(x-request-id)% google_req_id=%RESP(x-guploader-uploadid)% retry=%REQ(x-envoy-attempt-count)%\n"
                    common_http_protocol_options:
                      idle_timeout: 600s
                    http_filters:
                      - name: envoy.filters.http.ext_proc
                        typed_config:
                          "@type": type.googleapis.com/envoy.extensions.filters.http.ext_proc.v3.ExternalProcessor
                          grpc_service:
                            envoy_grpc:
                              cluster_name: ext_proc_local
                            timeout: 5s
                          failure_mode_allow: false
                          processing_mode:
                            request_header_mode: SEND
                            response_header_mode: SEND
                            request_body_mode: NONE
                            response_body_mode: NONE
                            request_trailer_mode: SKIP
                            response_trailer_mode: SKIP
                          message_timeout: 5s
                      - name: envoy.filters.http.router
                        typed_config:
                          "@type": type.googleapis.com/envoy.extensions.filters.http.router.v3.Router
                    route_config:
                      name: cloudcode
                      virtual_hosts:
                        - name: cloudcode
                          domains:
                            - "cloudcode-pa.googleapis.com"
                            - "daily-cloudcode-pa.googleapis.com"
                          routes:
                            - match: { prefix: "/" }
                              route:
                                cluster: cloudcode_api
                                timeout: 600s
                                retry_policy:
                                  retry_on: "retriable-status-codes"
                                  retriable_status_codes: [401]
                                  num_retries: 1
                                  per_try_timeout: 600s
                                auto_host_rewrite: true
                        - name: www_google
                          domains:
                            - "www.googleapis.com"
                          routes:
                            - match: { prefix: "/" }
                              route:
                                cluster: www_api
                                timeout: 600s
                                retry_policy:
                                  retry_on: "retriable-status-codes"
                                  retriable_status_codes: [401]
                                  num_retries: 1
                                  per_try_timeout: 600s
                                auto_host_rewrite: true
                        - name: play_google
                          domains:
                            - "play.googleapis.com"
                          routes:
                            - match: { prefix: "/" }
                              route:
                                cluster: play_api
                                timeout: 600s
                                retry_policy:
                                  retry_on: "retriable-status-codes"
                                  retriable_status_codes: [401]
                                  num_retries: 1
                                  per_try_timeout: 600s
                                auto_host_rewrite: true
                        - name: oauth2_google
                          domains:
                            - "oauth2.googleapis.com"
                          routes:
                            - match: { prefix: "/" }
                              route:
                                cluster: oauth2_api
                                timeout: 600s
                                retry_policy:
                                  retry_on: "retriable-status-codes"
                                  retriable_status_codes: [401]
                                  num_retries: 1
                                  per_try_timeout: 600s
                                auto_host_rewrite: true
      clusters:
        - name: cloudcode_api
          type: LOGICAL_DNS
          connect_timeout: 5s
          dns_lookup_family: V4_ONLY
          lb_policy: ROUND_ROBIN
          typed_extension_protocol_options:
            envoy.extensions.upstreams.http.v3.HttpProtocolOptions:
              "@type": type.googleapis.com/envoy.extensions.upstreams.http.v3.HttpProtocolOptions
              explicit_http_config:
                http2_protocol_options: {}
          load_assignment:
            cluster_name: cloudcode_api
            endpoints:
              - lb_endpoints:
                  - endpoint:
                      address:
                        socket_address:
                          address: cloudcode-pa.googleapis.com
                          port_value: 443
          transport_socket:
            name: envoy.transport_sockets.tls
            typed_config:
              "@type": type.googleapis.com/envoy.extensions.transport_sockets.tls.v3.UpstreamTlsContext
              sni: cloudcode-pa.googleapis.com
              common_tls_context:
                validation_context:
                  trusted_ca:
                    filename: /etc/ssl/certs/ca-certificates.crt
        - name: www_api
          type: LOGICAL_DNS
          connect_timeout: 5s
          dns_lookup_family: V4_ONLY
          lb_policy: ROUND_ROBIN
          load_assignment:
            cluster_name: www_api
            endpoints:
              - lb_endpoints:
                  - endpoint:
                      address:
                        socket_address:
                          address: www.googleapis.com
                          port_value: 443
          transport_socket:
            name: envoy.transport_sockets.tls
            typed_config:
              "@type": type.googleapis.com/envoy.extensions.transport_sockets.tls.v3.UpstreamTlsContext
              sni: www.googleapis.com
              common_tls_context:
                validation_context:
                  trusted_ca:
                    filename: /etc/ssl/certs/ca-certificates.crt
        - name: play_api
          type: LOGICAL_DNS
          connect_timeout: 5s
          dns_lookup_family: V4_ONLY
          lb_policy: ROUND_ROBIN
          load_assignment:
            cluster_name: play_api
            endpoints:
              - lb_endpoints:
                  - endpoint:
                      address:
                        socket_address:
                          address: play.googleapis.com
                          port_value: 443
          transport_socket:
            name: envoy.transport_sockets.tls
            typed_config:
              "@type": type.googleapis.com/envoy.extensions.transport_sockets.tls.v3.UpstreamTlsContext
              sni: play.googleapis.com
              common_tls_context:
                validation_context:
                  trusted_ca:
                    filename: /etc/ssl/certs/ca-certificates.crt
        - name: oauth2_api
          type: LOGICAL_DNS
          connect_timeout: 5s
          dns_lookup_family: V4_ONLY
          lb_policy: ROUND_ROBIN
          load_assignment:
            cluster_name: oauth2_api
            endpoints:
              - lb_endpoints:
                  - endpoint:
                      address:
                        socket_address:
                          address: oauth2.googleapis.com
                          port_value: 443
          transport_socket:
            name: envoy.transport_sockets.tls
            typed_config:
              "@type": type.googleapis.com/envoy.extensions.transport_sockets.tls.v3.UpstreamTlsContext
              sni: oauth2.googleapis.com
              common_tls_context:
                validation_context:
                  trusted_ca:
                    filename: /etc/ssl/certs/ca-certificates.crt
        - name: ext_proc_local
          type: STATIC
          connect_timeout: 1s
          lb_policy: ROUND_ROBIN
          typed_extension_protocol_options:
            envoy.extensions.upstreams.http.v3.HttpProtocolOptions:
              "@type": type.googleapis.com/envoy.extensions.upstreams.http.v3.HttpProtocolOptions
              explicit_http_config:
                http2_protocol_options: {}
          load_assignment:
            cluster_name: ext_proc_local
            endpoints:
              - lb_endpoints:
                  - endpoint:
                      address:
                        socket_address:
                          address: 127.0.0.1
                          port_value: 9000
{{- end }}
---
{{- if eq (include "tank-operator.renderHot" .) "true" }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: antigravity-api-proxy
  namespace: {{ include "tank-operator.orchestratorNamespace" . }}
  labels:
    app.kubernetes.io/name: antigravity-api-proxy
spec:
  replicas: {{ .Values.apiProxy.replicas | default 1 }}
  revisionHistoryLimit: 0
  selector:
    matchLabels:
      app.kubernetes.io/name: antigravity-api-proxy
  template:
    metadata:
      labels:
        app.kubernetes.io/name: antigravity-api-proxy
        azure.workload.identity/use: "true"
    spec:
      serviceAccountName: {{ .Values.apiProxy.serviceAccount }}
      containers:
        - name: envoy
          image: {{ .Values.apiProxy.envoyImage | quote }}
          args:
            - "-c"
            - "/etc/envoy/envoy.yaml"
            - "--service-cluster"
            - "antigravity-api-proxy"
          ports:
            - name: https
              containerPort: 8443
            - name: admin
              containerPort: 9901
          volumeMounts:
            - name: envoy-config
              mountPath: /etc/envoy
              readOnly: true
            - name: leaf-tls
              mountPath: /etc/envoy/tls
              readOnly: true
          readinessProbe:
            tcpSocket:
              port: 8443
            initialDelaySeconds: 3
            periodSeconds: 10
        - name: ext-proc
          image: "{{ .Values.apiProxy.image.repository }}:{{ .Values.apiProxy.image.tag }}"
          imagePullPolicy: {{ .Values.apiProxy.image.pullPolicy }}
          ports:
            - name: grpc
              containerPort: 9000
            - name: metrics
              containerPort: 9100
          env:
            - name: PROXY_PROVIDER
              value: antigravity
            - name: METRICS_PORT
              value: "9100"
            - name: ANTIGRAVITY_CREDENTIALS_FILE
              value: /etc/antigravity-credentials/{{ .Values.session.antigravityCredentialsSecretKey | default "antigravity-oauth-token" }}
            - name: AZURE_KEYVAULT_URL
              value: {{ .Values.credentialRefresher.keyVaultUrl | quote }}
            - name: ANTIGRAVITY_CREDENTIALS_KV_KEY
              value: {{ include "tank-operator.antigravityCredentialsKvKey" . | quote }}
            - name: AZURE_FEDERATED_TOKEN_FILE
              value: /var/run/secrets/azure/tokens/azure-identity-token
            - name: AZURE_AUTHORITY_HOST
              value: https://login.microsoftonline.com/
          envFrom:
            # apiProxyConfigSecret carries AZURE_CLIENT_ID/TENANT_ID (KV
            # write-back identity) and ANTIGRAVITY_CLIENT_SECRET (the Google
            # installed-app OAuth secret, KV-sourced so it stays out of git).
            - secretRef:
                name: {{ include "tank-operator.apiProxyConfigSecret" . }}
          volumeMounts:
            - name: antigravity-credentials
              mountPath: /etc/antigravity-credentials
              readOnly: true
      volumes:
        - name: envoy-config
          configMap:
            name: antigravity-api-proxy-envoy
        - name: leaf-tls
          secret:
            secretName: antigravity-api-proxy-leaf
            optional: true
        - name: antigravity-credentials
          secret:
            secretName: {{ .Values.session.antigravityCredentialsSecret | default "antigravity-credentials" }}
            optional: true
{{- end }}
{{- /*
Disruption protection only makes sense once apiProxy.replicas > 1; at a
single replica a minAvailable=1 PDB wedges node drains (see pdb.yaml for
the same reasoning on the orchestrator). Rendered alongside the
Deployments (hot); the file-level isTestEnv gate already excludes slots.
*/}}
{{- if and (eq (include "tank-operator.renderHot" .) "true") (gt (int (.Values.apiProxy.replicas | default 1)) 1) }}
{{- range $app := list "claude-api-proxy" "claude-secondary-api-proxy" "codex-api-proxy" "antigravity-api-proxy" }}
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ $app }}
  namespace: {{ include "tank-operator.orchestratorNamespace" $ }}
  labels:
    app.kubernetes.io/name: {{ $app }}
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ $app }}
{{- end }}
{{- end }}
---
{{- end }}
