apiVersion: v1
kind: Service
metadata:
  name: mcp-azure-admin
  namespace: mcp-azure
spec:
  type: ClusterIP
  selector:
    app: mcp-azure-admin
  ports:
    - name: http
      port: 80
      targetPort: proxy
