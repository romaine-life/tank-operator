{
  "name": "tank-agent-runner",
  "private": true,
  "version": "0.1.0",
  "description": "Long-lived pod-side runner that drives @anthropic-ai/claude-agent-sdk and persists canonical events to Cosmos.",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "node --test dist/**/*.test.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.2.0",
    "@azure/cosmos": "^4.5.1",
    "@azure/identity": "^4.7.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
